export class QuestionCategory {
    constructor(text, info, color) {
            this.name = text,
            this.info = info,
            this.color = color,
            this.questions = []
    }
}